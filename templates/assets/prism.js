// Minimal Prism.js - just enough for syntax highlighting
// This is a placeholder - in production, you'd use the full Prism.js library
(function() {
    if (typeof window !== 'undefined' && window.Prism) {
        // Prism is already loaded
        return;
    }
    
    // Basic syntax highlighting would go here
    // For now, we'll rely on the CSS classes
    console.log('Prism.js placeholder loaded');
})();
